<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Exports directory: /Android/data/com.ah.acr.messagebox/files/exports/ -->
    <external-files-path
        name="exports"
        path="exports" />

    <!-- Fallback: all external files -->
    <external-files-path
        name="external_files"
        path="." />

    <!-- 음성 녹음 (getCacheDir) -->
    <cache-path
        name="cache_files"
        path="." />

    <!-- 내부 파일 (getFilesDir) -->
    <files-path
        name="internal_files"
        path="." />
</paths>